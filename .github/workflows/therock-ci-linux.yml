name: TheRock CI Linux

on:
  workflow_call:
    inputs:
      cmake_options:
        type: string
      projects_to_test:
        type: string
      package_version:
        type: string
        required: true
      amdgpu_families:
        type: string
        default: "gfx94X-dcgpu"
      artifact_group:
        type: string
      test_runs_on:
        type: string
        default: "linux-gfx942-1gpu-core42-ossci-rocm"
      build_runs_on:
        type: string
        default: "azure-linux-scale-rocm"
      enable_rccl:
        type: boolean
        default: false
        description: "When true, builds with RCCL enabled and runs RCCL-specific test jobs instead of the standard test job."

permissions:
  contents: read

jobs:
  therock-build-linux:
    name: Build Linux Packages
    runs-on: ${{ inputs.build_runs_on }}
    permissions:
      id-token: write
    container:
      image: ghcr.io/rocm/therock_build_manylinux_x86_64@sha256:a382085df3ba2419b58aa9051350883a0d0b732a4bc0a4ef60458f8161bb08c6 # 2026-06-11
      options: -v /runner/config:/home/awsconfig/
    strategy:
      fail-fast: true
    env:
      CACHE_DIR: ${{ github.workspace }}/.container-cache
      # The ccache.conf will be written by setup_ccache.py before this gets used.
      CCACHE_CONFIGPATH: ${{ github.workspace }}/.ccache/ccache.conf
      AMDGPU_FAMILIES: ${{ inputs.amdgpu_families }}
      TEATIME_FORCE_INTERACTIVE: 0
      AWS_SHARED_CREDENTIALS_FILE: /home/awsconfig/credentials.ini
      THEROCK_REQUIRE_HIP_YAML_ENTRIES: 1
    steps:
      - name: "Checking out repository for rocm-systems"
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0

      - name: Checkout TheRock repository
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          repository: "ROCm/TheRock"
          path: "TheRock"
          ref: 177bc45e201c996e1a780e3348fd49ca2c439b8e # 2026-06-25 commit

      - name: Install python deps
        run: |
          pip install -r TheRock/requirements.txt

      # safe.directory must be set before Runner health status
      - name: Adjust git config
        run: |
          git config --global --add safe.directory $PWD
          git config fetch.parallel 10

      - name: Setup ccache
        run: |
          ./TheRock/build_tools/setup_ccache.py \
            --config-preset "github-oss-dev" \
            --dir "$(dirname $CCACHE_CONFIGPATH)" \
            --local-path "$CACHE_DIR/ccache"

      - name: Runner health status
        run: |
          ./TheRock/build_tools/health_status.py

      - name: Fetch sources
        timeout-minutes: 30
        run: |
          ./TheRock/build_tools/fetch_sources.py --jobs 12 --no-include-rocm-systems

      - name: Patch rocm-systems
        run: |
          # Remove patches here if they cannot be applied cleanly, and they have not been deleted from TheRock repo
          # rm ./TheRock/patches/amd-mainline/rocm-systems/0003-Use-is_versioned-true-consistently-in-both-Comgr-Loa.patch

      - name: Configure Projects
        env:
          amdgpu_families: ${{ env.AMDGPU_FAMILIES }}
          package_version: ${{ inputs.package_version }}
          # When enable_rccl is false, RCCL is explicitly disabled so the standard
          # build does not include it. When true, the flag is omitted and RCCL builds
          # normally.
          extra_cmake_options: >-
            -DTHEROCK_ROCM_SYSTEMS_SOURCE_DIR=../
            ${{ !inputs.enable_rccl && '-DTHEROCK_ENABLE_RCCL=OFF' || '' }}
            ${{ inputs.cmake_options }}
          BUILD_DIR: build
        run: |
          python3 TheRock/build_tools/github_actions/build_configure.py

      - name: Build therock-artifacts and therock-dist
        run: cmake --build TheRock/build --target therock-artifacts therock-dist -- -k 0

      - name: Report
        if: ${{ !cancelled() }}
        run: |
          echo "Full SDK du:"
          echo "------------"
          du -h -d 1 TheRock/build/dist/rocm
          echo "Artifacts:"
          echo "----------"
          du -h -d 1 TheRock/build/artifacts
          echo "CCache Stats:"
          echo "-------------"
          ccache -s -v
          tail -v -n +1 .ccache/compiler_check_cache/* > TheRock/build/logs/ccache_compiler_check_cache.log

      - name: Configure AWS Credentials
        if: ${{ always() && !github.event.pull_request.head.repo.fork }}
        uses: aws-actions/configure-aws-credentials@7474bc4690e29a8392af63c5b98e7449536d5c3a # v4.3.1
        with:
          aws-region: us-east-2
          role-to-assume: >-
            ${{ inputs.enable_rccl
              && 'arn:aws:iam::692859939525:role/therock-artifacts-external'
              || 'arn:aws:iam::692859939525:role/therock-ci-external' }}

      - name: Push artifacts
        run: |
          python3 TheRock/build_tools/artifact_manager.py push \
            --run-id ${{ github.run_id }} \
            --build-dir TheRock/build \
            --stage all

      - name: Post Build Upload
        if: always()
        run: |
          python3 TheRock/build_tools/github_actions/post_build_upload.py \
            --run-id ${{ github.run_id }} \
            --artifact-group ${{ env.AMDGPU_FAMILIES }} \
            --build-dir TheRock/build \
            --upload

  # ── Standard (non-RCCL) test job ─────────────────────────────────────────────
  therock-test-linux:
    name: "Test"
    needs: [therock-build-linux]
    if: ${{ !inputs.enable_rccl && inputs.amdgpu_families != 'gfx950-dcgpu' }}
    uses: ./.github/workflows/therock-test-packages.yml
    with:
      projects_to_test: ${{ inputs.projects_to_test }}
      amdgpu_families: ${{ inputs.amdgpu_families }}
      test_runs_on: ${{ inputs.test_runs_on }}
      platform: "linux"

  # ── RCCL test jobs (only active when enable_rccl: true) ──────────────────────
  therock-test-linux-rccl-multi-node:
    name: "Test RCCL multi-node"
    # TODO: Re-enable gfx94X-dcgpu and gfx950-dcgpu multi-node tests once the clusters are stable again.
    if: false
    permissions:
      contents: read
      id-token: write
    needs: [therock-build-linux]
    uses: ./.github/workflows/therock-rccl-test-packages-multi-node.yml
    with:
      amdgpu_families: ${{ inputs.amdgpu_families }}
      artifact_group: ${{ inputs.artifact_group }}
      test_runs_on: ruby-linux-slurm-scale-runner
      artifact_run_id: ${{ github.run_id }}

  therock-test-linux-rccl-single-node:
    name: "Test RCCL single-node"
    # TODO(TheRock#3288): Re-enable gfx950-dcgpu runners once we get more capacity
    if: ${{ inputs.enable_rccl && inputs.amdgpu_families != 'gfx950-dcgpu' }}
    needs: [therock-build-linux]
    uses: ./.github/workflows/therock-rccl-test-packages-single-node.yml
    with:
      amdgpu_families: ${{ inputs.amdgpu_families }}
      artifact_group: ${{ inputs.artifact_group }}
      test_runs_on: linux-gfx942-8gpu-ossci-rocm
      artifact_run_id: ${{ github.run_id }}

